var config = {
  spWidth: 740,//SPブレークポイント(PC最小幅)(px)
  windowResizeDelay: 100,//ウィンドウのリサイズに合わせる処理までの遅延(ms)
  showingDelayInterval: 60,//表示アニメーションのアイテムごとの間隔(ms)
  inviewOffset: -100//インビューイベントの発生する距離
};

export default config;